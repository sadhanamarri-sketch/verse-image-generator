import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  // Change this to your own reverse-domain package name before publishing.
  appId: 'com.sadhanamarri.scripturepaper',
  appName: 'Scripture Paper',
  webDir: 'dist',
  bundledWebRuntime: false,
};

export default config;
